<%= form_with(model: room) do |form| %>
  <% if room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>
      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.text_field :user_id, type: "hidden", value: current_user.id %>
  </div>
  <div>
    <%= form.text_field :seconduser_id, style: "display: block", value: @user.id, type: "hidden" %>
  </div>
  <div>
    <%= form.text_field :email, style: "display: block", value: current_user.email, type: "hidden" %>
  </div>
  <div>
    <%= form.text_field :secondemail, style: "display: block", value: @user.email, type: "hidden" %>
  </div>
  <div>
    <%= form.submit "Message this User"%>
  </div>
<% end %>
